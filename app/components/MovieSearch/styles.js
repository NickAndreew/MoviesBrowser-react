import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    margin: 20,
    resizeMode: 'contain'
  }, 
  inputSearch: {
    borderRadius: 20
  }
});

export { styles };